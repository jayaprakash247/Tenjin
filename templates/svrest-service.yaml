apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svtrest 
spec:
  selector:
    app: {{ .Release.Name }}-svtrest  
  ports:
    - protocol: TCP
      port: 8080  
      targetPort: 8080
  type: ClusterIP  
