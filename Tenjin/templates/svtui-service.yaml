apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svtui
spec:
  selector:
    app: {{ .Release.Name }}-svtui  
  ports:
    - protocol: TCP
      port: {{ .Values.svtui.ports[0].servicePort }}  
      targetPort: {{ .Values.svtui.ports[0].containerPort }}  
  type: ClusterIP  
