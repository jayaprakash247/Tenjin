apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Tenjin.fullname" . }}-test-connection"
  labels:
    {{- include "Tenjin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Tenjin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
